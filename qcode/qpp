#/bin/bash

BASE=`basename $1 .q`
GPUNAME=".${BASE}_gpu.q"
../preprocess.native $1 > "${GPUNAME}"
if [ "$2" = "debug" ]; 
then
  echo "!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! DEBUG !!!!!!!!!!!!!!!!!!!!!!!!!!!!";  
  gdb --args q "${GPUNAME}" 
else 
  q "${GPUNAME}" 
fi 
